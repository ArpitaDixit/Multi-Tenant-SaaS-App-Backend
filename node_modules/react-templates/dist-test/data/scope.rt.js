define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    function repeatItem1(items, item, itemIndex) {
        return React.createElement('span', { 'key': 'i-' + itemIndex }, 'Item:#', itemIndex, ' = ', item);
    }
    function scopeItems2() {
        var items = [
            'a',
            'b',
            'c'
        ];
        return React.createElement.apply(this, [
            'div',
            {},
            _.map(items, repeatItem1.bind(this, items))
        ]);
    }
    return function () {
        return React.createElement('div', {}, scopeItems2.apply(this, []), React.createElement('span', {}, typeof items == 'undefined' ? 'items not in scope' : 'items in scope'), React.createElement('span', {}, typeof item == 'undefined' ? 'item not in scope' : 'item in scope'));
    };
});