define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    function scopeUser1() {
        var user = {
            first: 'Jack',
            last: 'Sparrow'
        };
        return React.createElement('div', {}, '\n    ', user.first, '\n');
    }
    return function () {
        return scopeUser1.apply(this, []);
    };
});