define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    function scopeGetCurrentActiveUsers1() {
        var getCurrentActiveUsers = this.getCurrentActiveUsers;
        return React.createElement('div', { 'key': 'active-users' }, React.createElement('span', {}, 'some text'));
    }
    return function () {
        return getCurrentActiveUsers().length ? scopeGetCurrentActiveUsers1.apply(this, []) : null;
    };
});